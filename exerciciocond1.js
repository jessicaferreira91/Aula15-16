const respostaDoUsuario = prompt("Digite o número que você quer testar")
const numero = (2)

if (numero % 2 === 0) {
  console.log("Passou no teste.")
} else {
  console.log("Não passou no teste.")
}

/* 1. O código faz uma pergunta ao usuário e imprime a resposta no navegador se é verdadeiro ou falso.
2. Os números que imprimem no navegador que passou no teste são os números pares.
3.  Os números que imprimem no navegador que não passou no teste são os números impares. */

